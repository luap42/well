<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>
  <body class="bg-light">
    <nav class="navbar bg-white border-bottom mb-3 p-1">
      <div class="container">
        <a href="<%= root_path %>" class="navbar-brand fw-bold">Well</a>
      </div>
    </nav>

    <main class="container my-3">
      <div class="card login-card shadow-lg">
        <div class="card-body">
          <div class="p-3">
            <%= yield %>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
